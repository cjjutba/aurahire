# Slice 1.1 - Monorepo Init Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` (separate session) or `superpowers:subagent-driven-development` (this session) to implement this plan task-by-task.

**Goal:** Convert the current single-app Next.js project into a Turborepo + pnpm-workspaces monorepo. Establish `apps/web` (Next.js 16 frontend), `apps/api` (NestJS backend with Fastify + Swagger + health check), `packages/shared` (placeholder for Zod schemas), and `packages/db` (placeholder for Drizzle schema). After this slice, `pnpm dev` from the repo root starts both apps simultaneously.

**Architecture:** Two deployable applications + two shared packages, orchestrated by Turborepo. See `docs/main/architecture.md` for the full system view and `docs/main/project-structure.md` for the canonical folder layout.

**Tech Stack:** pnpm 9 workspaces, Turborepo 2, Next.js 16, React 19, Tailwind v4, NestJS 10 (Fastify adapter), TypeScript 5, Pino logger, Helmet, `@nestjs/swagger`. See `docs/main/tech-stack.md` for full dependency justification.

**Sprint reference:** `docs/main/sprint-plan.md` § Day 1 → Slice 1.1

**Estimated wall time:** 90 min (executing agent) + ~10 min (human pre-flight + verification)

---

## Definition of Done

This slice is complete when ALL of the following are true:

- [ ] Repo root has `package.json` (workspace coordinator), `pnpm-workspace.yaml`, `turbo.json`, `tsconfig.base.json`
- [ ] `apps/web/` contains the previously-root Next.js code (with adjusted `package.json` and `tsconfig.json` extending base)
- [ ] `apps/api/` contains a working NestJS app with Fastify adapter, Pino logger, Helmet, Swagger UI at `/api/docs`, and a `GET /api/health` endpoint
- [ ] `packages/shared/` and `packages/db/` exist as buildable empty packages
- [ ] `pnpm install` from repo root completes successfully
- [ ] `pnpm type-check` from repo root passes for all packages
- [ ] **Human verifies:** `pnpm dev` starts both apps; `localhost:3000` (frontend) and `localhost:3333/api/health` (backend) respond; `localhost:3333/api/docs` shows Swagger UI
- [ ] Human commits the slice with message: `chore: monorepo init (slice 1.1)`

---

## Hard Rules (from CLAUDE.md)

The executing agent MUST NOT:

- Run any dev server (`pnpm dev`, `next dev`, `nest start`, etc.) - the human runs these
- Run any Docker command (`docker compose ...`, `docker run`, `docker exec`) - the human runs these
- Run any database command (`drizzle-kit push`, migrations, seeds) - Slice 1.2 territory anyway
- `git push` or `git commit` to remote - the human commits

The executing agent MAY:

- Run `pnpm install`, `pnpm add`, `pnpm tsc --noEmit`, `pnpm lint`, `pnpm format`
- Read any file in the repo
- Write/edit any source file or config file
- Use Supabase MCP read-only operations (`list_projects`, `get_project`, `list_tables`)

---

## Pre-flight (Tasks 0a-0c)

### Task 0a: Read Next.js 16 monorepo docs

**Why:** `AGENTS.md` mandates reading the bundled Next.js 16 docs before writing Next.js code, because APIs/conventions may differ from training data.

**Files to read (no edits):**

- `node_modules/next/dist/docs/01-app/01-getting-started/02-project-structure.md`
- `node_modules/next/dist/docs/01-app/01-getting-started/01-installation.md`

**Step 1:** Use the `Read` tool on both paths.

**Step 2:** Confirm: does Next.js 16 require any specific monorepo config (e.g., `transpilePackages`, `outputFileTracingRoot`)? Note any constraints in the executing agent's working memory.

**Output of this task:** mental model of any Next.js 16 monorepo nuances. No file changes.

---

### Task 0b: Verify Supabase project state via MCP

**Why:** Confirm we have a Supabase project to point at in later slices. Read-only inventory; no mutations.

**Step 1:** Call `mcp__plugin_supabase_supabase__list_projects`.

**Expected:** one project with `name: "aurahire"`, `status: "ACTIVE_HEALTHY"`, project ref `fzjvalmouygmmnrgpgtg` (or whatever the user has).

**Step 2:** Call `mcp__plugin_supabase_supabase__get_project_url` with that project ID. Save the URL.

**Step 3:** Call `mcp__plugin_supabase_supabase__list_tables` with that project ID and `schemas: ["public"]`.

**Expected:** empty array (no tables yet - that's Slice 1.2).

**Output of this task:** confirmation that Supabase is reachable; project URL noted for env-setup. No file changes.

---

### Task 0c: Confirm pre-flight with human

**Why:** Slice 1.1 ends with the human running `pnpm dev`. We need pnpm 9 + Docker services + (later) populated env files.

**Step 1:** Print this exact message to the user and wait for confirmation:

```
Pre-flight check before Slice 1.1:
1. Is `pnpm --version` ≥ 9.0.0? (if not, run `npm install -g pnpm@9`)
2. Is Docker Desktop running and `docker compose -f docker-compose.dev.yml ps` shows aurahire-mailpit + aurahire-redis as Up?
   (if not, run `docker compose -f docker-compose.dev.yml up -d` from repo root)

Reply "go" when both green, or describe what's blocking.
```

**Step 2:** Do not proceed past Task 0c until human confirms.

---

## Phase A - Root Scaffolding (Tasks 1-4)

### Task 1: Create root `package.json`

**Files:**

- Create: `package.json` (overwriting the existing single-app one - but back up the deps first)
- Source of original deps (already in repo): existing `package.json`

**Step 1:** Read existing `package.json` to see current frontend deps. They will move to `apps/web/package.json` later.

**Step 2:** Replace root `package.json` with:

```json
{
  "name": "aurahire",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "format": "prettier --write \"**/*.{ts,tsx,md,json,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md,json,yml,yaml}\"",
    "clean": "turbo run clean && rm -rf node_modules .turbo"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "prettier": "^3.4.2",
    "prettier-plugin-tailwindcss": "^0.6.9",
    "typescript": "^5.7.2"
  }
}
```

**Step 3:** Delete `package-lock.json` from the repo root (npm lockfile from `create-next-app`; pnpm will generate `pnpm-lock.yaml` on install). Use `rm /Users/cjjutba/Projects/aurahire/package-lock.json`.

**Verification:** `cat package.json` shows the new content.

---

### Task 2: Create `pnpm-workspace.yaml`

**Files:**

- Create: `pnpm-workspace.yaml`

**Step 1:** Write file with content:

```yaml
packages:
  - "apps/*"
  - "packages/*"
```

**Verification:** file exists at repo root with the two-line content above.

---

### Task 3: Create `turbo.json`

**Files:**

- Create: `turbo.json`

**Step 1:** Write file with content:

```json
{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env*", "tsconfig.base.json"],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^build"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "lint": {
      "outputs": []
    },
    "type-check": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "clean": {
      "cache": false
    }
  }
}
```

**Verification:** `cat turbo.json` shows the schema reference + `dev`/`build`/`lint`/`type-check`/`clean` tasks.

---

### Task 4: Create `tsconfig.base.json`

**Files:**

- Create: `tsconfig.base.json`

**Step 1:** Write file with content:

```json
{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "incremental": true,
    "isolatedModules": true
  }
}
```

Each app/package's own `tsconfig.json` will extend this and override `module`/`moduleResolution`/`jsx` per-environment.

**Verification:** `cat tsconfig.base.json` shows the strict-mode preset.

---

## Phase B - Move Next.js to `apps/web/` (Tasks 5-9)

### Task 5: Create `apps/web/` directory and move Next.js source files

**Files (move using `mv` via Bash, NOT delete-then-recreate):**

Use `mkdir -p apps/web` first, then move:

- `app/` → `apps/web/app/`
- `public/` → `apps/web/public/`
- `next.config.ts` → `apps/web/next.config.ts`
- `next-env.d.ts` → `apps/web/next-env.d.ts`
- `eslint.config.mjs` → `apps/web/eslint.config.mjs`
- `postcss.config.mjs` → `apps/web/postcss.config.mjs`

**Step 1:** Run from repo root:

```bash
mkdir -p apps/web
mv app apps/web/app
mv public apps/web/public
mv next.config.ts apps/web/next.config.ts
mv next-env.d.ts apps/web/next-env.d.ts
mv eslint.config.mjs apps/web/eslint.config.mjs
mv postcss.config.mjs apps/web/postcss.config.mjs
```

**Step 2:** Delete `.next/` build artifact (will regenerate):

```bash
rm -rf .next
```

**Verification:** `ls apps/web/` shows `app/`, `public/`, `next.config.ts`, `next-env.d.ts`, `eslint.config.mjs`, `postcss.config.mjs`.

**Note:** The original `tsconfig.json` at root is NOT moved here - we'll write a new `apps/web/tsconfig.json` in Task 7. Delete the old root tsconfig.json:

```bash
rm tsconfig.json
```

---

### Task 6: Create `apps/web/package.json`

**Files:**

- Create: `apps/web/package.json`

**Step 1:** Write file with content:

```json
{
  "name": "@aurahire/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbo --port 3000",
    "build": "next build",
    "start": "next start --port 3000",
    "lint": "next lint",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "next": "16.2.4",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.0",
    "@types/node": "^20.17.10",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "eslint": "^9.17.0",
    "eslint-config-next": "16.2.4",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.2"
  }
}
```

**Verification:** `cat apps/web/package.json` matches.

---

### Task 7: Create `apps/web/tsconfig.json`

**Files:**

- Create: `apps/web/tsconfig.json`

**Step 1:** Write file with content:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "esnext",
    "moduleResolution": "bundler",
    "jsx": "preserve",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "noEmit": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./*"],
      "@aurahire/shared": ["../../packages/shared/src"],
      "@aurahire/shared/*": ["../../packages/shared/src/*"],
      "@aurahire/db": ["../../packages/db/src"],
      "@aurahire/db/*": ["../../packages/db/src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules", ".next", "dist"]
}
```

**Verification:** `cat apps/web/tsconfig.json` matches; references to `@/`, `@aurahire/shared`, `@aurahire/db` are present.

---

### Task 8: Update `apps/web/next.config.ts` for monorepo

**Files:**

- Modify: `apps/web/next.config.ts`

**Step 1:** Read current `apps/web/next.config.ts`. Likely content:

```ts
import type { NextConfig } from "next";
const nextConfig: NextConfig = {};
export default nextConfig;
```

**Step 2:** Replace with:

```ts
import type { NextConfig } from "next";
import path from "path";

const nextConfig: NextConfig = {
  // For monorepo: tell Next where to root file-tracing for serverless bundling
  outputFileTracingRoot: path.join(__dirname, "../../"),
  // Allow imports from workspace packages
  transpilePackages: ["@aurahire/shared", "@aurahire/db"],
  experimental: {
    // Enable server actions caching reset between requests in dev
    typedRoutes: false,
  },
};

export default nextConfig;
```

**Step 3:** Verify Next.js 16 recognizes these options. If `typedRoutes` or any field is deprecated in Next 16 (verify against the docs read in Task 0a), remove it.

**Verification:** file compiles when type-checked in Task 19.

---

### Task 9: Update `apps/web/eslint.config.mjs` (verify still valid in subdir)

**Files:**

- Read: `apps/web/eslint.config.mjs`

**Step 1:** Read the file. Confirm it imports `eslint-config-next` correctly and references local files only (no `../node_modules` or `../tsconfig.json` paths). If any path needs updating, fix it.

**Step 2:** Most likely no changes needed. Move on.

**Verification:** `cat apps/web/eslint.config.mjs` shows valid flat-config exports.

---

## Phase C - NestJS Scaffold in `apps/api/` (Tasks 10-17)

### Task 10: Create `apps/api/package.json`

**Files:**

- Create: `apps/api/package.json`

**Step 1:** Write file with content:

```json
{
  "name": "@aurahire/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nest start --watch --debug --preserveWatchOutput",
    "build": "nest build",
    "start": "node dist/main.js",
    "start:prod": "node dist/main.js",
    "lint": "eslint \"src/**/*.ts\"",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.15",
    "@nestjs/core": "^10.4.15",
    "@nestjs/platform-fastify": "^10.4.15",
    "@nestjs/config": "^3.3.0",
    "@nestjs/swagger": "^8.0.7",
    "@fastify/helmet": "^12.0.1",
    "fastify": "^4.28.1",
    "nestjs-pino": "^4.1.0",
    "pino": "^9.5.0",
    "pino-pretty": "^11.3.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.4.9",
    "@nestjs/schematics": "^10.2.3",
    "@types/node": "^20.17.10",
    "ts-loader": "^9.5.2",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.7.2"
  }
}
```

**Verification:** `cat apps/api/package.json` matches.

---

### Task 11: Create `apps/api/nest-cli.json`

**Files:**

- Create: `apps/api/nest-cli.json`

**Step 1:** Write file with content:

```json
{
  "$schema": "https://json.schemastore.org/nest-cli",
  "collection": "@nestjs/schematics",
  "sourceRoot": "src",
  "compilerOptions": {
    "deleteOutDir": true
  }
}
```

**Verification:** `cat apps/api/nest-cli.json` matches.

---

### Task 12: Create `apps/api/tsconfig.json`

**Files:**

- Create: `apps/api/tsconfig.json`

**Step 1:** Write file with content:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "commonjs",
    "moduleResolution": "node",
    "declaration": true,
    "removeComments": true,
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    "allowSyntheticDefaultImports": true,
    "sourceMap": true,
    "outDir": "./dist",
    "baseUrl": "./",
    "paths": {
      "@/*": ["src/*"],
      "@aurahire/shared": ["../../packages/shared/src"],
      "@aurahire/shared/*": ["../../packages/shared/src/*"],
      "@aurahire/db": ["../../packages/db/src"],
      "@aurahire/db/*": ["../../packages/db/src/*"]
    }
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
```

**Verification:** `cat apps/api/tsconfig.json` matches.

---

### Task 13: Create `apps/api/tsconfig.build.json`

**Files:**

- Create: `apps/api/tsconfig.build.json`

**Step 1:** Write file with content:

```json
{
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "test", "dist", "**/*.spec.ts", "**/*.test.ts"]
}
```

**Verification:** `cat apps/api/tsconfig.build.json` matches.

---

### Task 14: Create `apps/api/src/main.ts` (Fastify + Swagger + Pino + Helmet)

**Files:**

- Create: `apps/api/src/main.ts`

**Step 1:** Write file with content:

```ts
import { NestFactory } from "@nestjs/core";
import {
  FastifyAdapter,
  NestFastifyApplication,
} from "@nestjs/platform-fastify";
import { DocumentBuilder, SwaggerModule } from "@nestjs/swagger";
import { ValidationPipe, VersioningType } from "@nestjs/common";
import { Logger } from "nestjs-pino";
import helmet from "@fastify/helmet";
import { AppModule } from "./app.module";

async function bootstrap() {
  const app = await NestFactory.create<NestFastifyApplication>(
    AppModule,
    new FastifyAdapter({ logger: false }), // Pino is the logger
    { bufferLogs: true },
  );

  app.useLogger(app.get(Logger));

  // Security headers
  await app.register(helmet, {
    contentSecurityPolicy: {
      directives: {
        defaultSrc: [`'self'`],
        styleSrc: [`'self'`, `'unsafe-inline'`],
        imgSrc: [`'self'`, "data:", "validator.swagger.io"],
        scriptSrc: [`'self'`, `https: 'unsafe-inline'`],
      },
    },
  });

  // CORS
  const allowedOrigins = (
    process.env.ALLOWED_ORIGINS ?? "http://localhost:3000"
  )
    .split(",")
    .map((o) => o.trim());
  app.enableCors({
    origin: allowedOrigins,
    credentials: true,
  });

  // Global prefix + versioning
  app.setGlobalPrefix("api", { exclude: ["health"] });
  app.enableVersioning({ type: VersioningType.URI, defaultVersion: "1" });

  // Global validation pipe (will be replaced by ZodValidationPipe in Slice 1.3)
  app.useGlobalPipes(new ValidationPipe({ transform: true, whitelist: true }));

  // Swagger
  const config = new DocumentBuilder()
    .setTitle("AuraHire API")
    .setDescription(
      "Backend API for AuraHire - Explainable + Fair AI-Powered Recruitment",
    )
    .setVersion("0.1.0")
    .addBearerAuth({ type: "http", scheme: "bearer", bearerFormat: "JWT" })
    .build();
  const document = SwaggerModule.createDocument(app, config);
  SwaggerModule.setup("api/docs", app, document, {
    swaggerOptions: { persistAuthorization: true },
  });

  const port = Number(process.env.PORT ?? 3333);
  await app.listen(port, "0.0.0.0");

  // eslint-disable-next-line no-console
  console.log(`AuraHire API running at http://localhost:${port}`);
  // eslint-disable-next-line no-console
  console.log(`Swagger UI at http://localhost:${port}/api/docs`);
}

void bootstrap();
```

**Verification:** TypeScript compiles in Task 19.

---

### Task 15: Create `apps/api/src/app.module.ts`

**Files:**

- Create: `apps/api/src/app.module.ts`

**Step 1:** Write file with content:

```ts
import { Module } from "@nestjs/common";
import { ConfigModule } from "@nestjs/config";
import { LoggerModule } from "nestjs-pino";
import { HealthModule } from "./health/health.module";

@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true, cache: true }),
    LoggerModule.forRoot({
      pinoHttp: {
        transport:
          process.env.NODE_ENV !== "production"
            ? {
                target: "pino-pretty",
                options: { singleLine: true, translateTime: "SYS:HH:MM:ss" },
              }
            : undefined,
        level: process.env.LOG_LEVEL ?? "info",
        autoLogging: true,
        redact: ["req.headers.authorization", "req.headers.cookie"],
      },
    }),
    HealthModule,
  ],
})
export class AppModule {}
```

**Verification:** TypeScript compiles in Task 19.

---

### Task 16: Create `apps/api/src/health/` (controller + module)

**Files:**

- Create: `apps/api/src/health/health.module.ts`
- Create: `apps/api/src/health/health.controller.ts`

**Step 1:** Write `apps/api/src/health/health.controller.ts`:

```ts
import { Controller, Get } from "@nestjs/common";
import { ApiOperation, ApiResponse, ApiTags } from "@nestjs/swagger";

@ApiTags("health")
@Controller({ path: "health", version: undefined })
export class HealthController {
  @Get()
  @ApiOperation({ summary: "Service health check" })
  @ApiResponse({ status: 200, description: "Service is healthy" })
  check(): {
    status: "ok";
    uptime: number;
    version: string;
    timestamp: string;
  } {
    return {
      status: "ok",
      uptime: process.uptime(),
      version: process.env.APP_VERSION ?? "0.1.0",
      timestamp: new Date().toISOString(),
    };
  }
}
```

**Step 2:** Write `apps/api/src/health/health.module.ts`:

```ts
import { Module } from "@nestjs/common";
import { HealthController } from "./health.controller";

@Module({
  controllers: [HealthController],
})
export class HealthModule {}
```

**Verification:** Both files exist; TypeScript compiles in Task 19.

**Note:** `health` is exposed at `/api/health` (NOT `/api/v1/health`) because `main.ts` excludes it from the global prefix and versioning.

Wait - actually, re-reading `main.ts`: we have `app.setGlobalPrefix("api", { exclude: ["health"] })` - that excludes `health` from BOTH prefix `api` AND from the versioning. So `health` is at `/health` (no prefix). For Railway health probes that's fine. But the project structure doc says `/api/health`. Let me adjust:

In `main.ts`, change `app.setGlobalPrefix("api", { exclude: ["health"] })` to `app.setGlobalPrefix("api")`. Then in `health.controller.ts`, add `@Controller({ path: "health" })` (no `version: undefined`) which will make it `/api/v1/health` by default.

Actually we want `/api/health` (no version) per `architecture.md`. Best approach: exclude version on health by setting `@Version(VERSION_NEUTRAL)` on the controller method.

Revised health.controller.ts (replace the file):

```ts
import { Controller, Get, Version, VERSION_NEUTRAL } from "@nestjs/common";
import { ApiOperation, ApiResponse, ApiTags } from "@nestjs/swagger";

@ApiTags("health")
@Controller("health")
export class HealthController {
  @Get()
  @Version(VERSION_NEUTRAL)
  @ApiOperation({ summary: "Service health check" })
  @ApiResponse({ status: 200, description: "Service is healthy" })
  check(): {
    status: "ok";
    uptime: number;
    version: string;
    timestamp: string;
  } {
    return {
      status: "ok",
      uptime: process.uptime(),
      version: process.env.APP_VERSION ?? "0.1.0",
      timestamp: new Date().toISOString(),
    };
  }
}
```

And in `main.ts`, REMOVE the `exclude` option:

Change:

```ts
app.setGlobalPrefix("api", { exclude: ["health"] });
```

to:

```ts
app.setGlobalPrefix("api");
```

Net result: `GET /api/health` returns the health JSON (because `Version(VERSION_NEUTRAL)` opts out of `/v1/`).

**Verification:** After Task 19 type-check passes and Task 20 human boots the server, `curl http://localhost:3333/api/health` returns `{"status":"ok",...}`.

---

### Task 17: Create `apps/api/src/.gitkeep` placeholders for future modules folder

**Files:**

- Create: `apps/api/src/modules/.gitkeep`
- Create: `apps/api/src/common/.gitkeep`
- Create: `apps/api/src/ai/.gitkeep`
- Create: `apps/api/src/queue/.gitkeep`
- Create: `apps/api/src/cron/.gitkeep`
- Create: `apps/api/src/email/.gitkeep`
- Create: `apps/api/src/storage/.gitkeep`
- Create: `apps/api/src/audit/.gitkeep`
- Create: `apps/api/src/config/.gitkeep`
- Create: `apps/api/src/db/.gitkeep`

**Step 1:** Create empty `.gitkeep` files in each directory above so git tracks the folder structure even though it's empty.

**Verification:** `ls -la apps/api/src/` shows all the planned folders from `docs/main/project-structure.md`.

---

## Phase D - Shared Packages (Tasks 18-19)

### Task 18: Create `packages/shared/`

**Files:**

- Create: `packages/shared/package.json`
- Create: `packages/shared/tsconfig.json`
- Create: `packages/shared/src/index.ts`

**Step 1:** Write `packages/shared/package.json`:

```json
{
  "name": "@aurahire/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schemas/*": "./src/schemas/*.ts",
    "./enums/*": "./src/enums/*.ts",
    "./constants/*": "./src/constants/*.ts",
    "./types/*": "./src/types/*.ts"
  },
  "scripts": {
    "lint": "eslint \"src/**/*.ts\"",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
```

**Step 2:** Write `packages/shared/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "esnext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "noEmit": true,
    "baseUrl": "./src"
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules"]
}
```

**Step 3:** Write `packages/shared/src/index.ts`:

```ts
// AuraHire shared schemas, enums, constants, and types.
// Slice 1.3 will populate this with auth and onboarding schemas.

export const AURAHIRE_SHARED_VERSION = "0.1.0";
```

**Verification:** Three files exist; `pnpm --filter @aurahire/shared type-check` passes after install.

---

### Task 19: Create `packages/db/`

**Files:**

- Create: `packages/db/package.json`
- Create: `packages/db/tsconfig.json`
- Create: `packages/db/src/index.ts`

**Step 1:** Write `packages/db/package.json`:

```json
{
  "name": "@aurahire/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "lint": "eslint \"src/**/*.ts\"",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.4",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "drizzle-kit": "^0.27.0",
    "typescript": "^5.7.2"
  }
}
```

**Step 2:** Write `packages/db/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "esnext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "noEmit": true,
    "baseUrl": "./src"
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules"]
}
```

**Step 3:** Write `packages/db/src/index.ts`:

```ts
// AuraHire database schema (Drizzle).
// Slice 1.2 will populate this with the full 15-table schema + RLS policies.

export const AURAHIRE_DB_VERSION = "0.1.0";
```

**Verification:** Three files exist; `pnpm --filter @aurahire/db type-check` passes after install.

---

## Phase E - Root Files (Tasks 20-21)

### Task 20: Update root `.gitignore` for monorepo

**Files:**

- Modify: `.gitignore`

**Step 1:** Read existing `.gitignore`. Likely the create-next-app default.

**Step 2:** Replace with:

```gitignore
# Dependencies
node_modules/
.pnpm-store/

# Build outputs
**/.next/
**/.turbo/
**/dist/
**/build/

# TypeScript
**/*.tsbuildinfo
**/next-env.d.ts

# Env files
.env
.env.local
.env.*.local
**/.env
**/.env.local
**/.env.*.local
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/

# Vercel
.vercel/

# Misc
*.pem
.cache/
coverage/
```

**Verification:** `cat .gitignore` matches.

---

### Task 21: Create `.env.example` at root

**Files:**

- Create: `.env.example`

**Step 1:** Write file with content:

```bash
# AuraHire - environment variables template
# Copy contents into apps/web/.env.local and apps/api/.env, fill with real values.
# DO NOT COMMIT actual .env files.

# ───────────────────────────────────────────────────────────────────────
# apps/web/.env.local - frontend
# ───────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
NEXT_PUBLIC_API_URL=http://localhost:3333
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# ───────────────────────────────────────────────────────────────────────
# apps/api/.env - backend
# ───────────────────────────────────────────────────────────────────────
NODE_ENV=development
PORT=3333

# Database (Supabase pooler - port 6543 for transaction mode)
DATABASE_URL=postgresql://postgres.PROJECT_REF:PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres

# Supabase (for auth JWT validation + storage)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Redis (Docker Compose dev or Railway addon prod)
REDIS_URL=redis://localhost:6379

# OpenAI
OPENAI_API_KEY=sk-proj-your_key_here
OPENAI_MODEL=gpt-4o-mini

# Email - Mailpit (dev) / Resend (prod)
SMTP_HOST=localhost
SMTP_PORT=1025
RESEND_API_KEY=re_your_key_here
FROM_EMAIL=onboarding@resend.dev

# CORS
ALLOWED_ORIGINS=http://localhost:3000

# Logging
LOG_LEVEL=info
```

**Verification:** `cat .env.example` matches.

---

## Phase F - Install + Type Check (Tasks 22-23)

### Task 22: Run `pnpm install`

**Files:** none (modifies `pnpm-lock.yaml` only)

**Step 1:** Run from repo root:

```bash
pnpm install
```

**Expected output:** lockfile generated; all four packages (root, apps/web, apps/api, packages/shared, packages/db) install dependencies. May take 30-60s on first run.

**Step 2:** Verify lockfile exists: `ls -la pnpm-lock.yaml` should show a non-zero file.

**Step 3:** If `pnpm install` errors:

- Read error message carefully
- Most common: missing peer dep - add it
- Most common: version conflict - pin to compatible versions per `tech-stack.md`
- If unresolvable, halt and ask human

**Verification:** No errors in output; `pnpm-lock.yaml` exists.

---

### Task 23: Run type-check across all packages

**Step 1:** Run from repo root:

```bash
pnpm type-check
```

**Expected:** all packages pass type-check. The root `turbo type-check` runs `tsc --noEmit` in each app/package.

**Step 2:** If type errors:

- Read errors per-package
- Most likely failures:
  - Next.js needs Tailwind v4 plugin types - add to `apps/web/postcss.config.mjs` if missing
  - NestJS missing `reflect-metadata` import - add at top of `main.ts` if not already
  - Path alias not resolving - verify `tsconfig.json` paths match `pnpm-workspace.yaml` package names
- Fix and re-run

**Verification:** Final output shows `✓` for each task in turbo output.

---

## Phase G - Human Verification (Tasks 24-25)

### Task 24: Hand off to human - start dev servers

**Step 1:** Print to user:

```
Slice 1.1 implementation complete. Please verify by running from repo root:

  pnpm dev

Expected output (interleaved):

  apps/web:dev: ▲ Next.js 16.2.4
  apps/web:dev:   - Local:        http://localhost:3000
  apps/api:dev: AuraHire API running at http://localhost:3333
  apps/api:dev: Swagger UI at http://localhost:3333/api/docs

Then verify:
  1. Open http://localhost:3000 - should render the existing Next.js placeholder page
  2. Open http://localhost:3333/api/health - should return JSON: { "status": "ok", "uptime": ..., "version": "0.1.0", "timestamp": "..." }
  3. Open http://localhost:3333/api/docs - should render Swagger UI with the "health" tag listed

Reply "verified" when all three are green, or describe any failure.
```

**Step 2:** Wait for confirmation.

---

### Task 25: Hand off to human - commit

**Step 1:** Print to user:

```
Verified. Please commit Slice 1.1 from repo root:

  git add .
  git status         # review what's staged
  git commit -m "chore: monorepo init (slice 1.1)

Convert single-app project into Turborepo + pnpm-workspaces monorepo:
- apps/web - Next.js 16 frontend (existing code moved here)
- apps/api - NestJS 10 backend with Fastify, Pino, Helmet, Swagger UI
- packages/shared - placeholder for Zod schemas (Slice 1.3)
- packages/db - placeholder for Drizzle schema (Slice 1.2)
- Root: turbo.json, pnpm-workspace.yaml, tsconfig.base.json, .env.example
- Root pnpm dev runs both apps simultaneously
- Backend exposes /api/health and /api/docs

Refs: docs/main/sprint-plan.md § Slice 1.1, docs/plans/2026-05-02-slice-1.1-monorepo-init.md"

Then push to remote:

  git push origin main
```

(The agent does NOT run these commands - the human does, per CLAUDE.md hard rules.)

**Step 2:** Wait for confirmation that the commit happened. Then mark Slice 1.1 ✅ in `docs/plans/README.md`.

---

## Slice Done - Next Steps

After Slice 1.1 is verified and committed:

1. The planning agent (this session or a fresh one) writes the **Slice 1.2 plan** at `docs/plans/2026-05-02-slice-1.2-database-schema.md`. Slice 1.2 covers: full Drizzle schema in `packages/db/src/schema.ts`, RLS policy SQL files, applying schema + policies to Supabase via MCP, seeding default `scoring_config` row.
2. Update `docs/plans/README.md` - change Slice 1.1 status to ✅ Complete; change Slice 1.2 status to 📝 Plan ready.

---

## Failure Modes & Recovery

| Symptom                                                | Likely Cause                                       | Fix                                                                       |
| ------------------------------------------------------ | -------------------------------------------------- | ------------------------------------------------------------------------- |
| `pnpm install` fails with peer dep error               | Version mismatch                                   | Pin to versions in this plan                                              |
| `pnpm dev` shows only one app starting                 | `turbo.json` `dev` task missing `persistent: true` | Re-check Task 3                                                           |
| Frontend errors "Cannot find module 'next'"            | Move from Task 5 incomplete                        | Re-verify `ls apps/web/`                                                  |
| Backend errors "Cannot find module 'reflect-metadata'" | Missing import in `main.ts`                        | Add `import "reflect-metadata";` at top of `main.ts` (NestJS requires it) |
| `/api/docs` returns 404                                | Swagger setup never registered                     | Re-check Task 14                                                          |
| `/api/health` returns 404                              | Versioning conflict                                | Re-check Task 16 - `@Version(VERSION_NEUTRAL)` on the GET method          |
| Type-check fails for `@aurahire/shared` import         | Path alias incorrect                               | Verify `apps/web/tsconfig.json` and `apps/api/tsconfig.json` paths        |
| `transpilePackages` not recognized in next.config.ts   | Next.js 16 API change                              | Read Next.js 16 docs (Task 0a); update accordingly                        |

---

## Cross-References

- `CLAUDE.md` - hard rules (no servers, no docker, no DB, no deploy, no global installs)
- `AGENTS.md` - Next.js 16 caveat (read bundled docs first)
- `docs/main/sprint-plan.md` § Slice 1.1 - sprint context
- `docs/main/architecture.md` - system architecture (full)
- `docs/main/tech-stack.md` - every dependency + version rationale
- `docs/main/project-structure.md` - folder conventions
- `docs/main/best-practices.md` - engineering standards
- `docs/main/env-setup.md` § Step 5 - Docker Compose for Mailpit + Redis
- `docs/plans/README.md` - index of all slice plans
