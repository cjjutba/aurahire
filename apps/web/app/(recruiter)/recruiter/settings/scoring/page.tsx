import { ScoringPageClient } from "./_scoring-page-client";

export const metadata = { title: "Scoring · Settings" };

export default function RecruiterSettingsScoringPage() {
  return <ScoringPageClient />;
}
