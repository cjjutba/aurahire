import { MembersPageClient } from "./_members-page-client";

export const metadata = { title: "Team Members · Settings" };

export default function RecruiterSettingsMembersPage() {
  return <MembersPageClient />;
}
