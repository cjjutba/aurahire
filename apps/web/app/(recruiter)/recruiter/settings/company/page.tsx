import { CompanyPageClient } from "./_company-page-client";

export const metadata = { title: "Company · Settings" };

export default function RecruiterSettingsCompanyPage() {
  return <CompanyPageClient />;
}
