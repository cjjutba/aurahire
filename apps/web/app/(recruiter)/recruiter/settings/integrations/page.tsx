import { IntegrationsPageClient } from "./_integrations-page-client";

export const metadata = { title: "Integrations · Settings" };

export default function RecruiterSettingsIntegrationsPage() {
  return <IntegrationsPageClient />;
}
